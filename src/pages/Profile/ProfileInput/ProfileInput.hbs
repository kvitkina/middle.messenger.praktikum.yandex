{{#with input}}
	<div class='profile-input'>
		<label class='profile-input__label'>{{label}}</label>
		<input
			class='profile-input__item'
			type='{{type}}'
			value='{{value}}'
			id='{{name}}'
			name='{{name}}'
			placeholder='{{label}}'
			required
		/>
	</div>
{{/with}}